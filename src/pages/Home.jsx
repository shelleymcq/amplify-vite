import React from "react";

const Home = () => {
  return (
    <div>
      <h1>Vite + React + Amplify + Cypress</h1>
      <h2>Implement Testing with GitHub Actions</h2>
      <h3>This element will fail a test intentionally. What happens?</h3>
    </div>
  );
};

export default Home;
